services:
  redis-cache-store: 
    container_name: redis-store
    image: redis
    restart: always
    ports:
      - '6379:6379'
    volumes: 
      - cache:/data
volumes:
  cache:
    driver: local